# Cashflow Sentinel

Phase 3 foundation: real lead capture, Supabase persistence, Resend transactional emails, internal notification, admin pipeline and scheduled follow-up.

Configure .env.example in Vercel. Run supabase/schema.sql in the chosen Supabase project, then deploy main and test the visitor-to-follow-up flow.